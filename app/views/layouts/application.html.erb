<!DOCTYPE html>
<html>
  <head>
    <title>TestGuru</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

  <% if logged_in? %>
    <p class="nav user">Приветствуем, <%= current_user.name %> <%=link_to "Guru",root_path %> | <%= link_to 'Log out', session_path(current_user.id),method: :delete, data: {confirm: "Вы уверены?"} %></p>
  <% else %>
    <p><%= link_to 'Login', login_path %> | <%= link_to 'Signup', signup_path %></p>
  <% end %>


    <%= yield %>
    <%= render 'layouts/footer' %>

  </body>
</html>
