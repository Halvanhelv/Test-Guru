<div class="row">
  <div class="col">
    <h1><%= t('.header') %></h1>
    <table class="table">
      <tr>
        <th><%= t('.title') %></td>

        <th><%= t('.category') %></td>
        <th><%= t('.level') %></td>
        <th><%= t('.questions') %></td>
        <th><%= t('.action') %></td>
      </tr>

        <%= render @tests %>

    </table>
    <p><%= link_to t('.create_test'), new_admin_test_path %></p>

  </div>
</div>